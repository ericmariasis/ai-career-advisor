# SSL configuration for production
# HTTPS server block (apex)
server {
    listen 443 ssl http2;
    server_name careercodeadvisor.com;

    # SSL certificate configuration
    ssl_certificate /etc/letsencrypt/live/careercodeadvisor.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/careercodeadvisor.com/privkey.pem;

    # SSL security settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-SHA256:ECDHE-RSA-AES256-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # Security headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options nosniff always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    include /etc/nginx/conf.d/common-routes.inc;
}

# HTTPS redirect for www to apex
server {
    listen 443 ssl http2;
    server_name www.careercodeadvisor.com;

    ssl_certificate /etc/letsencrypt/live/careercodeadvisor.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/careercodeadvisor.com/privkey.pem;

    return 301 https://careercodeadvisor.com$request_uri;
}

# HTTP to HTTPS redirect (force apex)
server {
    listen 80;
    server_name careercodeadvisor.com www.careercodeadvisor.com;
    return 301 https://careercodeadvisor.com$request_uri;
}