# nginx/default.conf - Default server block for unmatched domains
server {
    listen 80 default_server;

    # Return 444 for unmatched domains (connection closed without response)
    return 444;
}
